---
import { ExternalLink } from '@lucide/astro';

interface Props {
  size?: number;
  class?: string;
}

const { size = 14, class: className } = Astro.props;
---

<ExternalLink
  size={size}
  class:list={['external-link-icon', className]}
  aria-hidden="true"
/>
